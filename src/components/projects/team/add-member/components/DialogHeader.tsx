
import React from 'react';
import { DialogHeader, DialogTitle } from "@/components/ui/dialog";

const AddMemberDialogHeader: React.FC = () => {
  return (
    <DialogHeader>
      <DialogTitle>Add Team Member</DialogTitle>
    </DialogHeader>
  );
};

export default AddMemberDialogHeader;
