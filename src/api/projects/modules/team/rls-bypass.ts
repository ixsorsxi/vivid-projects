import { supabase } from '@/integrations/supabase/client';

// Empty placeholder - this file is only a reference and not actively used
// We keep it to avoid breaking imports

export const getTeamMembersAdmin = async () => {
  console.warn('rls-bypass.ts is deprecated and should not be used');
  return [];
};
